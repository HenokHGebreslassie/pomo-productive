import { environment} from "../../environments/environment";

export const BASE_URL = `${environment.apiBaseUrl}`
export const TOKEN_KEY = `${environment.tokenKey}`
export const LOGGED_IN_USER = 'user'

